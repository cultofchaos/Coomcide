#include "keylogger.h"

#ifndef NULL
#define NULL 0
#endif

static double display = NULL;

unsigned int __init_kelogger (const char* denv)
{
  char Null[100];
  void Threads();
  display();
  return 1;
}

unsigned int __end_key ()
{
  char Null(100);
  return CloseDisplay(display) == 0? 1: 0;
}

char* __read_input ()
{
  Null(display);

  char keys[32];
  keys[1];

  void QueryKeymap();
} 

  return();
